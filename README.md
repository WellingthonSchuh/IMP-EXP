# 📦 IMP-EXP (Import e Export)

## 📌 Descrição
Projeto criado para praticar conceitos de **modularização em JavaScript** utilizando **import/export**, além de configurar e usar o **Webpack** para gerar o arquivo `bundle.js`.  
Também foi feita a instalação e configuração de **plugins como o Babel**, para transpilar o código e garantir compatibilidade.

---

## 📂 Estrutura do projeto
- **SRC/**
  - `index.js`
  - `modulo1.js`
- **docs/**
  - `index.html`
  - **Assets/**
    - `bundle.js`
    - `bundle.js.map`
- `.gitignore`
- `package-lock.json`
- `package.json`
- `webpack.config.js`

---

## ⚙️ Funcionalidades
- Uso de **import/export** para dividir o código em módulos.  
- Configuração do **Webpack** para gerar o `bundle.js`.  
- Instalação e uso do **Babel** para transpilar o código.  
- Organização de pastas para separar código-fonte (`src`) e arquivos gerados (`docs/assets`).  

---

## 🛠️ Tecnologias utilizadas
- **HTML5** → estrutura básica da página  
- **JavaScript (ES6+)** → modularização com `import` e `export`  
- **Webpack** → empacotamento e geração do `bundle.js`  
- **Babel** → transpilar código moderno para maior compatibilidade  

---

## 🚀 Como visualizar
Este projeto não possui visualização funcional no GitHub Pages, pois foi feito apenas para prática de **Webpack** e **Babel**.  
Para rodar localmente:
1. Instale as dependências:
   ```bash
   npm install
   ```
2. Gere o bundle com o Webpack:
   npx webpack

3. Abra o arquivo docs/index.html em um navegador.

## 📚 Aprendizados
- **Modularização:** uso de `import` e `export` para organizar o código
- **Empacotamento:** geração de `bundle.js` com **Webpack**
- **Source maps:** criação de `bundle.js.map` para depuração
- **Transpilação:** configuração do **Babel** para compatibilidade com navegadores

---

## 👨‍💻 Autor
Feito por **Wellingthon Schuh**  
🔗 [LinkedIn](https://www.linkedin.com/in/wellingthonschuh)
